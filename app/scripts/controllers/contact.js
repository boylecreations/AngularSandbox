'use strict';

/**
 * @ngdoc function
 * @name SandboxApp.controller:ContactCtrl
 * @description
 * # AboutCtrl
 * Controller of the SandboxApp
 */
angular.module('SandboxApp')
  .controller('ContactCtrl', ['$scope', '$filter', function ($scope, $filter) {
 
  }]);
